# Local dev runtime state (the JSON store + uploaded document bytes) — never
# part of the deployed source. On Vercel persistence goes through a
# StorageAdapter instead (see apps/api/src/storage/types.ts); this directory
# is dev-only.
apps/api/data
!apps/api/data/.gitkeep

# Build output — regenerated by `buildCommand` on every deploy.
apps/web/dist
